<%= form_for(paytm_detail) do |f| %>
  <% if paytm_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(paytm_detail.errors.count, "error") %> prohibited this paytm_detail from being saved:</h2>

      <ul>
      <% paytm_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :phone_number %>
    <%= f.text_field :phone_number %>
  </div>

  <div class="field">
    <%= f.label :cust_id %>
    <%= f.number_field :cust_id %>
  </div>

  <div class="field">
    <%= f.label :status %>
    <%= f.check_box :status %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
