<%= form_for(product) do |f| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_name %>
    <%= f.text_field :product_name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.text_field :image %>
  </div>

  <div class="field">
    <%= f.label :over_all_rating %>
    <%= f.text_field :over_all_rating %>
  </div>

  <div class="field">
    <%= f.label :discount_available %>
    <%= f.check_box :discount_available %>
  </div>

  <div class="field">
    <%= f.label :total_quantity %>
    <%= f.number_field :total_quantity %>
  </div>

  <div class="field">
    <%= f.label :available %>
    <%= f.check_box :available %>
  </div>

  <div class="field">
    <%= f.label :vendor_detail_id %>
    <%= f.number_field :vendor_detail_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
